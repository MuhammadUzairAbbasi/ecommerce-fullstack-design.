// components/LoadingSpinner.jsx
import React from "react";
import { PulseLoader } from "react-spinners"; // Import the spinner component from react-spinners

const Loader = () => {
  return <PulseLoader />;
};

export default Loader;
